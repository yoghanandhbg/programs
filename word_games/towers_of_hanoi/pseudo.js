class Game{
  run(){
    // until all the discs are placed to rightmost position
      // ask the player for a disc to move
      // make sure it is valid (no larger one on smaller one)
      // make the move
  }
}
